import os
os.system("pkill chromium")
exit()

#command = "/usr/bin/sudo pkill chromium"
#import subprocess #
#process = subproces.Popen(command.split(), stdout=subprocess.PIPE) 
#output = process.communicate()[0]
#print output
